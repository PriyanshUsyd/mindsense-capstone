# Locked stack per build-reference.md Section 3. DRAFT — built to fill a
# Week 4 gap (no requirements file existed); version pins below are
# starting points, not verified against a real resolved environment yet.
fastapi
uvicorn[standard]
pydantic>=2
pandas
numpy
statsmodels
pyyaml
sqlite-utils  # optional convenience only; backend/db.py is still the only file allowed to import sqlite3 directly
pytest
pytest-asyncio
pytest-socket
ruff
httpx  # test-only: required by fastapi.testclient.TestClient to exercise backend/api/app.py.
       # Privacy spot-check (2026-09-05): no network calls by default (used only
       # in-process against our own TestClient/ASGI app in tests); no telemetry/
       # analytics; no install scripts or auto-updates; touches only synthetic
       # test fixtures, never participant data; BSD-3-Clause license; no
       # stdlib alternative (urllib doesn't implement the ASGI transport
       # TestClient needs). Decision: approved, test-only dependency.

# Explicitly excluded per build-reference.md's decisions log — do not add:
# scikit-learn
