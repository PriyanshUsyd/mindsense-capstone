"""Shared evidence contract package. See evidence.py and build-reference.md Section 5.

Rule (build-reference.md Section 8): if a file in data_pipeline/ needs to talk
to statistics/, it imports from contracts/ — never directly from another
role's folder.
"""
