# Integration & QA (Priyansh Khandelwal) — the merge boundary

Empty at Week 4 by design — this is where the FastAPI app itself lives once
enough of the other roles' contracts/logic exist to integrate against. The
OpenAPI export script (`scripts/export_openapi.py`, referenced by
`skills/frontend-react.md`) lives alongside this once the app exists.
